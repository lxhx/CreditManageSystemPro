@model CreditManageSystemPro.Admin.Models.Menu
@{
    ViewBag.Title = "Details";
}

<div class="col-sm-8">
                <div class="ibox float-e-margins">
                    <div class="ibox-title">
                        <h5>
                            菜单详情</h5>
                        &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;
                    </div>
                    <div class="ibox-content">
                        <form class="form-horizontal m-t" id="commentForm" method="post" action="@Url.Action("Add", "Menu")">
                            @*<div class="form-group">
            <label class="col-sm-3 control-label">父菜单名称：</label>
            <div class="col-sm-8">
                @Html.DropDownList("select", "0")
                <input id="parentMenuName" name="parentMenuName" type="text" class="form-control">
            </div>
        </div>*@
                        <div class="form-group">
                            <div class="col-sm-8">
                                @Html.HiddenFor(model => model.parentMenuId, new { @class = "form-control", @required = "required", @id = "parentMenuName" })
                            </div>
                        </div>    
                        <div class="form-group">
                            <label class="col-sm-3 control-label">
                                菜单名称：</label>
                            <div class="col-sm-8">
                                @Html.TextBoxFor(model => model.menuName, new { @class = "form-control", @required = "required" ,@id="menuName"})
                            </div>
                        </div>
                        <div class="form-group">
                            <label class="col-sm-3 control-label">
                                菜单Url：</label>
                            <div class="col-sm-8">
                                @Html.TextBoxFor(model => model.menuUrl, new { @class = "form-control", @required = "required", @id = "menuUrl" })
                            </div>
                        </div>
                        <div class="form-group">
                            <label class="col-sm-3 control-label">
                                描述：</label>
                            <div class="col-sm-8">
                                @Html.TextAreaFor(model => model.menuNote, new { @class = "form-control", @id = "menuDep" })
                            </div>
                        </div>
                        <div class="form-group">
                            <div class="col-sm-4 col-xs-offset-5">
                                <button class="btn btn-primary required" type="submit">
                                    提交</button>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;
                                <button class="btn btn-primary" type="button">
                                    取消</button>
                            </div>
                        </div>
                        </form>
                    </div>
                </div>
            </div>
